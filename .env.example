# SMTP credentials for the contact form (set these in .env.local for production)
SMTP_HOST=mail.weload.ro
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=office@weload.ro
SMTP_PASSWORD=

# Where contact-form submissions are delivered
CONTACT_TO=office@weload.ro
# From address used by the mailer (usually the same as SMTP_USER)
CONTACT_FROM=office@weload.ro
